<!-- router.get("/data",(req,res,nex/t)=>{
     Con.exec((err,data)=>{
         if(err) throw err;
        res.status(200).render('DataBase.pug',{record : data});
     }); 
 });

 app.get('/data', function(req, res){
     Con.find(function (err, data){
       res.render('DataBase.pug', {data : data});
     });    
   });
   module.exports = app; -->
